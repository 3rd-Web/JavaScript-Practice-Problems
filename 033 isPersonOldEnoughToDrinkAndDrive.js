// Write a function called isPersonOldEnoughToDrinkAndDrive. Given a person object, that contains an age property, isPersonOldEnoughToDrinkAndDrive
// returns true if the given person is old enough to legally drink and drive in the United States. Otherwise it returns false.

function isPersonOldEnoughToDrinkAndDrive(person) {
  // the person object contains an "age" property inside
  // Add your code after this line
  if (person.age) {
    return false;
  }
  return obj;
}

let obj = {
  age: 1,
};
let output = isPersonOldEnoughToDrinkAndDrive(obj);
console.log(output); // --> false
